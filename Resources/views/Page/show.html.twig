{% extends base_template %}

{% block content %}
    <div class="rid-page page show">
        <h2>{{ page.title }}</h2>
        {{ page.body|format_text(page.bodyFormatter)|raw }}
    </div>
{% endblock %}